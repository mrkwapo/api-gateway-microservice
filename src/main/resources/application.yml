spring:
  application:
    name: api-gateway
server:
  port: 8082
zuul:
  routes:
    product-api:
      path: /api/**
      #service-id: persist-big-data
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 2000       